[package]
name = "pdfmuse-wasm"
description = "pdfmuse binding shell — wasm-bindgen, see PER-59"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
pdfmuse-core = { path = "../pdfmuse-core" }
